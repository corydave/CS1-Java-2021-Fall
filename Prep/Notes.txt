MULTIPLE CHOICE:

1. JVM
2. String age = "three";
3. I only
4. There is only one error in this code that prevents it from compiling.
5. 6.0
6. Success! Divides evenly.
7. 4
8. ArrayIndexOutOfBoundsException

CODE:

import java.util.Scanner;

public class Loop01 {
  public static void main (String[] args) {
    
    // Create an instance of SCANNER
    Scanner scanner = new Scanner(System.in);
    
    // Get the 'upperLimit' from the user
    System.out.println("Enter the upper limit:");
    int upperLimit = scanner.nextInt();
    System.out.println();
    
    
    // INSERT YOUR CODE HERE
    
    for (int i = 0; i <= upperLimit; i+=2) {
      System.out.print(i + " ");
    }

  }
}






import java.util.Scanner;

public class Widgets {
  public static void main (String[] args) {

    // Create an instance of SCANNER
    Scanner scanner = new Scanner(System.in);
    
    // Get the 'numOfGallons' from the user
    System.out.println("Enter the number of widgets you are purchasing:");
    int numOfWidgets = scanner.nextInt();
    System.out.println();
    
    // INSERT YOUR CODE HERE
    // Calculate and display the cost of the widgets being purchased
    double totalCost = -1;
    
    if (numOfWidgets <= 144) {
      totalCost = numOfWidgets * .09;
    } else {
      totalCost = ((numOfWidgets - 144) * .04) + (144 * .09);
    }
    
    
    System.out.println("The total cost of the widget order is $" + totalCost);


    
    
  }
}










import java.util.Random;
import java.util.Scanner;

public class ArrayAverage {
  public static void main (String[] args) {
    
    // Create an instance of SCANNER
    Scanner scanner = new Scanner(System.in);
    
    // Ask the user for a seed and store it in 'seed'
    System.out.println("SEED:");
    int seed = scanner.nextInt();
    
    // Ask the user for a size of the array and store it in size
    System.out.println("\nSIZE:");
    int size = scanner.nextInt();
    
    // Create an instance of RANDOM
    Random random = new Random(seed);
    
    // Create an array of ints that is 20 elements long
    // Note that the size, 20, is a parameter for this method
    // You may change it while debugging
    int[] arr = createArray(random, size);
    
    // You can modify this code for controlled testing
    // Pick one of the following return statements and uncomment
    // it, but be sure to comment out the call to 'createArray()' on
    // line 24
    // int[] arr = {1, 1, 1};
    // int[] arr = {1, 2, 3};
    // int[] arr = {1, 2, 3, 4};

    System.out.println("\nArray: " + displayArray(arr));
    System.out.println("Average: " + computeAverage(arr));
    
  }

  public static double computeAverage(int[] arr) {
    
    // INSERT YOUR CODE HERE
    // This method should find the average of all the elements 
    // in the array. If there are no elements in the array, the
    // method should return 0.
    
    double total = 0;
    
    for (int i = 0; i < arr.length; i++) {
      
      total += arr[i];
      
    }
    
    // double avg = total/arr.length;
    // return avg;
    
    if (arr.length == 0 ) {
      return 0;
    } else {
      return total/arr.length;
    }
 
  }
  
  public static String displayArray(int[] arr) {

    if (arr.length > 0) {
        
        String str = "["    ;
        
        for (int i = 0; i < arr.length - 1; i++) {
          str += arr[i] + ", ";
        }
        
        str += arr[arr.length-1] + "]";

        return str;        

    } else {
        
        return "[]";
        
    }

  }
  
  public static int[] createArray(Random random, int size) {
    
    // Create an empty array of size 20
    int[] arr = new int[size];
    
    // Populate array with ints valued 1-100
    for (int i = 0; i < arr.length; i++) {
      arr[i] = random.nextInt(100) + 1;
    }      
    
    // Return the new array
    return arr;
    
  }

}







import java.util.Scanner;

public class Loop02 {
  public static void main (String[] args) {
    
    // Instantiate a SCANNER
    Scanner scanner = new Scanner(System.in);
    
    // Prompt user for an 'incrementor'
    System.out.println("Start at:");
    int starter = scanner.nextInt();

    System.out.println("Increment by:");
    int incrementor = scanner.nextInt();
    System.out.println();
    
    // INSERT YOUR CODE HERE
    
    for (int i = starter; i <= 300; i += incrementor) {
      System.out.println(i);
    }

  }
}




import java.util.Scanner;

public class Methods01 {
  public static void main (String[] args) {
  
    // Instantiate a SCANNER
    Scanner scanner = new Scanner(System.in);
    
    // Prompt the user for a number
    System.out.println("Enter a number:");
    int input = scanner.nextInt();
    
    // INSERT YOUR CODE HERE
    // 1. Make a call to the method 'hailstone' that takes in an int
    // 2. Return an int that satisfies the condition of 'hailstone' to the main method
    // 3. Output the value as specified

    System.out.println("Result: " + hailstone(input));
    
    
  }
  
  // INSERT YOUR CODE HERE FOR THE hailstone() METHOD

  // This method takes an int and returns the int divided by two if
  // the int is even, otherwise it returns three times the int plus 1

  public static int hailstone(int num) {
    
      if (num % 2 == 0) {
        return num / 2;
      } else {
        return (3 * num) + 1;
      }
  }
    
}




import java.util.Scanner;

public class Lipogrammer {
  public static void main (String[] args) {
    
     // Instantiate a SCANNER
    Scanner scanner = new Scanner(System.in);
    
    // Get the string from the user
    System.out.println("Enter a word or phrase:");
    String input = scanner.nextLine();
    
    System.out.println("\nResult of lipogrammer(): " + lipogrammer(input));
    
  }
  
  // This method should return true if there is an 'e' in the value
  // of 'input'. Otherwise, it should return false.
  public static boolean lipogrammer(String input) {
    if (input.indexOf('e') >= 0) {
      return false;
    } else {
      return true;
    }
  }
  
}




import java.util.Random;
import java.util.Scanner;

public class ArrayAverage {
  public static void main (String[] args) {
    
    // Create an instance of SCANNER
    Scanner scanner = new Scanner(System.in);
    
    // Ask the user for a seed and store it in 'seed'
    System.out.println("SEED:");
    int seed = scanner.nextInt();
    
    // Ask the user for a size of the array and store it in size
    System.out.println("\nSIZE:");
    int size = scanner.nextInt();
    
    // Create an instance of RANDOM
    Random random = new Random(seed);
    
    // Create an array of ints that is 20 elements long
    // Note that the size, 20, is a parameter for this method
    // You may change it while debugging
    int[] arr = createArray(random, size);
    
    // You can modify this code for controlled testing
    // Pick one of the following return statements and uncomment
    // it, but be sure to comment out the call to 'createArray()' on
    // line 24
    // int[] arr = {1, 1, 1};
    // int[] arr = {1, 2, 3};
    // int[] arr = {1, 2, 3, 4};

    System.out.println("\nArray: " + displayArray(arr));
    System.out.println("Average: " + computeAverage(arr));
    
  }

  public static double computeAverage(int[] arr) {
    
    // INSERT YOUR CODE HERE
    
    if (arr.length > 0) {
        // Find the average of the numbers in this array and return it.
        double average = 0;
        
        for (int i = 0; i < arr.length; i++) {
          average += arr[i];
        }
        
        return average/arr.length;
        
    } else {
        
        return 0;
        
    }
  }
  
  public static String displayArray(int[] arr) {

    if (arr.length > 0) {
        
        String str = "["    ;
        
        for (int i = 0; i < arr.length - 1; i++) {
          str += arr[i] + ", ";
        }
        
        str += arr[arr.length-1] + "]";

        return str;        

    } else {
        
        return "[]";
        
    }

  }
  
  public static int[] createArray(Random random, int size) {
    
    // Create an empty array of size 20
    int[] arr = new int[size];
    
    // Populate array with ints valued 1-100
    for (int i = 0; i < arr.length; i++) {
      arr[i] = random.nextInt(100) + 1;
    }      
    
    // Return the new array
    return arr;
    
  }

}
